idade, nome = 31, 'Douglas' # É possível declarar várias variáveis em uma mesma linha.
print(f'Meu nome é {nome} e eu tenho {idade} ano(s) de idade.') # "f" indica que a string contém variáveis, as quais são identificadas pelas chaves "{ }". 

# O tipo da variável é definido implicitamente através do dado atribuído à ela quando ela é criada.
# Não existem constantes no Python, porém, é uma convenção escrever os nomes das variáveis em MAIÚSCULO identificá-las como constantes.
# Exemplos:
#   ABS_PATH = '/home/user/scripts/'
#   ESTADOS = ["SP", "RJ", "SC", "RS"]